import preloader from '../../../assets/images/spinner.svg';

const Preloader = (props) => {
return <div style={{height: '100%' }}><img src={preloader} style={{width: '100%' }} /></div>
};
export default Preloader;